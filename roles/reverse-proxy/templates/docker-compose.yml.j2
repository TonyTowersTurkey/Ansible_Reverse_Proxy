version: '3.9'

services:
  app:
    image: {{ reverse_proxy_npm_image }}
    restart: unless-stopped
    ports:
      - "80:80"
      - "81:81"
      - "443:443"
    volumes:
      - ./data:/data
      - ./letsencrypt:/etc/letsencrypt
